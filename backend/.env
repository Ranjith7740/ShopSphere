DB_URL=jdbc:mysql://localhost:3306/shopsphere?useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=team03

# Must be at least 32 characters (HS256 requires a >=256-bit key). Keep secret in real environments.
JWT_SECRET=replace-with-a-long-random-secret-value-of-32-chars-or-more
JWT_EXPIRATION_MS=3600000
